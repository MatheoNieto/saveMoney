import {CustomFonts} from '@theme/constants';

const textVariants = {
  defaults: {
    color: 'textPrimary',
    fontFamily: CustomFonts.Primary,
    letterSpacing: 0.049,
    fontSize: 14,
  },
};

export default textVariants;
